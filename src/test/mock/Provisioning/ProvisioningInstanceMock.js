'use strict';
import {Instance} from '../../../lib.compiled/Provisioning/Instance';

/**
 * Provisioning instance mock
 */
export class ProvisioningInstanceMock extends Instance {
  /**
   * @param {PropertyInstance} property
   */
  constructor(property) {
    super(property);
  }
}
