// THIS TEST WAS GENERATED AUTOMATICALLY ON Mon Feb 08 2016 11:57:30 GMT+0200 (EET)

'use strict';

import chai from 'chai';
import {SemVerStrategy} from '../../../../lib/Registry/Resolver/Strategy/SemVerStrategy';

// @todo: Add more advanced tests
suite('Registry/Resolver/Strategy/SemVerStrategy', function() {
  test('Class SemVerStrategy exists in Registry/Resolver/Strategy/SemVerStrategy', () => {
    chai.expect(SemVerStrategy).to.be.an('function');
  });
});
